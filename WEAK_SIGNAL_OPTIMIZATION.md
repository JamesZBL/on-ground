# 弱信号优化说明

## 概述

地铁环境（特别是隧道）中GPS信号很弱，甚至完全丢失。本应用针对这种情况实现了多种优化策略，确保在弱信号环境下仍能提供良好的用户体验。

## 优化策略

### 1. 自动降级策略

**工作原理**：
- 首先尝试高精度定位（GPS）
- 如果高精度定位失败，自动切换到低精度模式（网络定位、基站定位）
- 最多重试3次（可配置）

**优势**：
- 在GPS信号弱时仍能获取位置
- 自动适应不同信号环境
- 无需用户手动切换

### 2. 位置缓存机制

**工作原理**：
- 接受一定时间内的缓存位置（高精度30秒，低精度60秒）
- 减少定位请求次数，提高响应速度
- 在网络不稳定时使用缓存位置

**优势**：
- 减少定位失败
- 提高响应速度
- 节省电量

### 3. 位置历史记录

**工作原理**：
- 保存最近10个位置点（可配置）
- 记录每个位置的时间戳和精度
- 用于计算速度和方向

**用途**：
- 位置预测
- 速度计算
- 方向判断

### 4. 基于速度的位置预测

**工作原理**：
- 根据历史位置计算移动速度
- 当GPS信号丢失时，基于最后已知位置、速度和方向预测当前位置
- 仅在短时间内（1分钟内）使用预测位置

**优势**：
- 在隧道中短暂失去信号时仍能显示位置
- 提供连续的用户体验
- 自动恢复真实定位

### 5. 信号强度显示

**工作原理**：
- 根据定位精度判断信号强度
- 实时显示信号强度图标
- 精度 ≤ 20米：强信号 📶强
- 精度 ≤ 50米：中等信号 📶中
- 精度 ≤ 100米：弱信号 📶弱
- 精度 > 100米：很弱信号 📶很弱

**优势**：
- 用户了解当前信号状态
- 帮助判断位置准确性

### 6. 重试机制

**工作原理**：
- 定位失败时自动重试
- 高精度失败后尝试低精度
- 最多重试3次（可配置）

**优势**：
- 提高定位成功率
- 减少用户操作

### 7. 超时时间优化

**工作原理**：
- 高精度定位：15秒超时（默认10秒）
- 低精度定位：20秒超时
- 给弱信号环境更多时间获取位置

**优势**：
- 提高弱信号环境下的定位成功率
- 减少超时错误

## 配置选项

在 `config.js` 中可以配置弱信号优化参数：

```javascript
WEAK_SIGNAL: {
    MAX_RETRIES: 3,              // 最大重试次数
    HIGH_ACCURACY_TIMEOUT: 15000, // 高精度定位超时（毫秒）
    LOW_ACCURACY_TIMEOUT: 20000,  // 低精度定位超时（毫秒）
    MAX_CACHE_AGE_HIGH: 30000,    // 高精度缓存时间（毫秒）
    MAX_CACHE_AGE_LOW: 60000,     // 低精度缓存时间（毫秒）
    MAX_HISTORY_SIZE: 10,         // 位置历史记录数量
    MAX_LAST_POSITION_AGE: 120    // 最后位置最大年龄（秒）
}
```

## 使用场景

### 场景1：地铁隧道

**问题**：GPS信号完全丢失

**解决方案**：
1. 使用最后已知位置
2. 基于速度预测当前位置
3. 显示"估算位置"提示

### 场景2：信号不稳定

**问题**：GPS信号时有时无

**解决方案**：
1. 自动降级到低精度模式
2. 使用位置缓存
3. 重试机制提高成功率

### 场景3：定位精度差

**问题**：GPS信号弱，精度低（>100米）

**解决方案**：
1. 显示信号强度提示
2. 接受低精度位置
3. 使用位置预测提高准确性

## 性能影响

### 内存使用
- 位置历史记录：约1KB（10个位置点）
- 总体影响：可忽略

### 电量消耗
- 位置缓存减少定位请求
- 降级策略减少高精度定位时间
- 总体影响：降低电量消耗

### 响应速度
- 位置缓存提高响应速度
- 降级策略减少等待时间
- 总体影响：提高响应速度

## 最佳实践

1. **在进入隧道前**：确保已获取一次高精度位置
2. **信号恢复时**：应用会自动切换回高精度模式
3. **长时间无信号**：应用会显示"位置数据过期"，建议重新定位
4. **配置调整**：根据实际使用环境调整超时时间和重试次数

## 技术细节

### 位置预测算法

```javascript
// 计算速度
speed = distance / time

// 预测位置
predictedLat = lastLat + (directionLat * speed * timeSinceLastUpdate)
predictedLng = lastLng + (directionLng * speed * timeSinceLastUpdate)
```

### 信号强度判断

```javascript
if (accuracy <= 20) return '强';
if (accuracy <= 50) return '中';
if (accuracy <= 100) return '弱';
return '很弱';
```

## 注意事项

1. **位置预测仅用于短期**：超过1分钟不使用预测位置
2. **精度提示**：预测位置会显示"估算"标记
3. **自动恢复**：信号恢复后自动使用真实定位
4. **用户提示**：弱信号模式下会显示相应提示

## 未来改进

- [ ] 使用加速度计辅助定位
- [ ] 结合地铁时刻表优化预测
- [ ] 机器学习优化位置预测
- [ ] 离线地图支持
